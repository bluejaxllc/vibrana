# ─── Vibrana Frontend Environment Variables ───
# Copy this to .env and adjust as needed

# API endpoint (leave empty to auto-detect localhost)
VITE_API_URL=

# Force local API routing (set to 'true' for local dev)
VITE_USE_LOCAL=true
